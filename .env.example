# Claude API 키 (필수)
ANTHROPIC_API_KEY=sk-ant-...

# GitHub Actions에서 자동 주입되는 값 (로컬 실행 시 불필요)
# GITHUB_EVENT_PATH=/path/to/event.json
# PR_CHANGED_FILES=src/foo.js\nsrc/bar.js
# PR_DIFF=<full diff patch>
